# /skills:init-pack

## What it does
- (Re)generates `.cursor/skills/` baseline 5-pack:
  repo-bootstrap, tdd-go, ci-precommit, rules-vs-skills, release-check
- Adds domain skills for inventory_master

## Output
- File diffs + example invocations
